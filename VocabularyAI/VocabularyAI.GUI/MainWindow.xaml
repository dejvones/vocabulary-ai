<Window x:Class="VocabularyAI.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VocabularyAI.GUI" 
        xmlns:viewmodels="clr-namespace:VocabularyAI.GUI.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="540" Width="900">
    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="MaxHeight" Value="56"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.18"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="18"
                            SnapsToDevicePixels="True">
                            <ContentPresenter 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center" 
                                RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RoundedButtonStylePink" TargetType="Button" BasedOn="{StaticResource RoundedButtonStyle}">
            <Setter Property="Background" Value="#E91E63"/>
        </Style>
        <!-- Style for the Generate Words button -->
        <Style x:Key="GenerateButtonStyle" TargetType="Button" BasedOn="{StaticResource RoundedButtonStyle}">
            <Setter Property="Background" Value="#43A047"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="28,12"/>
            <Setter Property="MinWidth" Value="220"/>
            <Setter Property="Margin" Value="0,0,24,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.22"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border 
                            Background="{TemplateBinding Background}" 
                            CornerRadius="28"
                            SnapsToDevicePixels="True">
                            <ContentPresenter 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center" 
                                RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Style for the score badge -->
        <Style x:Key="ScoreBadgeStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF3CD"/>
            <Setter Property="BorderBrush" Value="#FFECB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="22,8"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid>
            <!-- Top colored bar -->
            <Border Height="8" VerticalAlignment="Top" Background="{Binding LastResultBrush}" CornerRadius="4,4,0,0" Margin="8,8,8,0"/>

            <!-- Top message bar for last result -->
            <Border Height="44" VerticalAlignment="Top" Margin="24,24,24,0"
            Background="{Binding LastResultBrush}" CornerRadius="10" Opacity="0.95">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding LastResultIcon}" FontSize="24" Margin="10,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding LastResultMessage}" FontSize="17" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Header controls -->
            <StackPanel Orientation="Horizontal" Margin="16,84,16,8" VerticalAlignment="Top" HorizontalAlignment="Left">
                <!-- Generate button -->
                <Button Command="{Binding GenerateWordsCommand}" Content="🔄 Generate Words" Style="{StaticResource GenerateButtonStyle}"/>
                <!-- Score badge -->
                <Border Style="{StaticResource ScoreBadgeStyle}" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Status}" Foreground="#795548" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" TextAlignment="Center"/>
                </Border>
                <!-- Level selection -->
                <ComboBox Style="{DynamicResource {x:Static ToolBar.ComboBoxStyleKey}}"
                          Margin="10,0,10,0"
                        Width="140" 
                        Height="44"
                        FontSize="18"
                        FontWeight="Bold"
                        Padding="10,0"
                        ItemsSource="{Binding Levels}"
                        SelectedItem="{Binding SelectedLevel, Mode=TwoWay}"
                        DisplayMemberPath="Name"
                        VerticalAlignment="Center"
                        />
                <!-- Topic input -->
                <Border Background="#FFF3E0" CornerRadius="14" Padding="10,0" VerticalAlignment="Center">
                    <TextBox Width="280"
                         Height="44"
                         FontSize="18"
                         FontWeight="Bold"
                         Padding="0"
                         Text="{Binding Topic, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         HorizontalContentAlignment="Left"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="#E65100"
                         />
                </Border>
            </StackPanel>

            <!-- Word selection area -->
            <Grid Margin="16,148,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <!-- Czech words -->
                <Border Grid.Column="0" Margin="0,0,8,0" Background="#F5F7FA" CornerRadius="10" Padding="12">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Czechs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button 
                                Command="{Binding DataContext.SelectCzechWordCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                                CommandParameter="{Binding Value}" 
                                Content="{Binding Value}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource RoundedButtonStyle}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="#1565C0"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
                <!-- English words -->
                <Border Grid.Column="1" Margin="8,0,0,0" Background="#F5F7FA" CornerRadius="10" Padding="12">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Englishs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button 
                                Command="{Binding DataContext.SelectEnglishWordCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" 
                                CommandParameter="{Binding Value}" 
                                Content="{Binding Value}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource RoundedButtonStylePink}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                        <Setter Property="Background" Value="#AD1457"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>

        <!-- Loading overlay -->
        <Grid Visibility="{Binding ShowLoading}"
              Background="#80000000" 
              Panel.ZIndex="100">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Background="White" CornerRadius="16" Padding="32,24" Opacity="0.95">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" >
                        <ProgressBar IsIndeterminate="True" Width="80" Height="18" Margin="0,0,16,0"/>
                        <TextBlock Text="Loading..." FontSize="18" FontWeight="Bold" Foreground="#333"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
