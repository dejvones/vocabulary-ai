<UserControl x:Class="VocabularyAI.GUI.Views.HistoryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VocabularyAI.GUI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <!-- Top colored bar -->
            <Border Height="8" VerticalAlignment="Top" Background="#2196F3" CornerRadius="4,4,0,0" Margin="8,8,8,0"/>

            <!-- Top message bar for last result -->
            <Border Height="44" VerticalAlignment="Top" Margin="8,8,8,0"
            Background="#2196F3" CornerRadius="10" Opacity="0.95">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="‼️" FontSize="24" Margin="10,0,6,0" VerticalAlignment="Center" Foreground="White"/>
                    <TextBlock Text="Tvá problémová slova" FontSize="17" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Main controls -->
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5*" />
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <!-- Back Button -->
                <Button Grid.Column="0" Command="{Binding NavigateToMenuCommand}"
                     HorizontalAlignment="Left"
                     VerticalAlignment="Top"
                     Style="{StaticResource GenerateButtonStyle}"
                     ToolTip="Back to Menu" Background="#2196F3">
                    <TextBlock Text="◀️ Menu" Foreground="White"/>
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
